{
  "name": "pf2e-quick-rolls",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rimraf module/scripts && mkdir -p module/scripts && touch module/scripts/.gitkeep",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "rimraf": "^5.0.5",
    "tsup": "^7.2.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
